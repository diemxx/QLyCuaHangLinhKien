<UserControl x:Class="ComputerStore_WPF.Views.UserControls.LichSuTimKiemUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#E8F1EC">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="LỊCH SỬ TÌM KIẾM" FontSize="22" FontWeight="Bold" Foreground="#2C3D38" VerticalAlignment="Center"/>
            <Button Content="LÀM MỚI" Command="{Binding RefreshCommand}" 
                    HorizontalAlignment="Right" Height="32" Padding="15,0" FontSize="12" FontWeight="Bold" 
                    Background="#2A9D8F" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </DockPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding DanhSach}" AutoGenerateColumns="False" IsReadOnly="True"
                  Background="#FFFFFF" Foreground="#2C3D38" RowBackground="#FFFFFF" 
                  AlternatingRowBackground="#F4F8F6" BorderBrush="#C2D6CE" BorderThickness="1" 
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E0EAE5" HeadersVisibility="Column">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#4A7C6B"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="8,5"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding MaLS}" Width="50"/>
                <DataGridTextColumn Header="Từ khóa tìm kiếm" Binding="{Binding TuKhoa}" Width="*"/>
                <DataGridTextColumn Header="Thời gian" Binding="{Binding ThoiGian, StringFormat='{}{0:dd/MM/yyyy HH:mm:ss}'}" Width="180"/>
                <DataGridTextColumn Header="Người tìm" Binding="{Binding TenNguoiTimKiem}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>